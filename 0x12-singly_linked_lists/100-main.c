#include <stdio.h>

/**
* main - outputs a quote
*
* Return: Always 0.
*/
int main(void)
{
printf("(A tortoise, having a pretty good sense of a hare's nature, challenges one to a race.)\n");
return (0);
}
